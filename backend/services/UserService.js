const createUser = () => {
  return new Promise((resolve, reject) => {
    try {
    } catch (error) {
      reject(error);
    }
  });
};

export default createUser; //xuất create user làm giá trị chính 
